<!--
  Add the following receiver element to your AndroidManifest.xml inside the <application> tag:

  <receiver
      android:name=".widget.NoContactWidgetReceiver"
      android:exported="true"
      android:label="@string/widget_name">
      <intent-filter>
          <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
      </intent-filter>
      <meta-data
          android:name="android.appwidget.provider"
          android:resource="@xml/nocontact_widget_info" />
  </receiver>

  Also add the WidgetPackage to your MainApplication.java/kt getPackages() method:

  packages.add(new WidgetPackage());

  Or in Kotlin:

  packages.add(WidgetPackage())
-->
